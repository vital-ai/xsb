boundmode(detached(false))
boundmode(status(running))
boundmode(alias(main))
varmode(0,detached(false))
varmode(0,status(running))
varmode(0,alias(main))
varmode(4,detached(false))
varmode(4,status(true))
varmode(3,detached(false))
varmode(3,status(exited(exit_check)))
varmode(2,detached(false))
varmode(2,status(false))
varmode(1,detached(false))
varmode(1,status(exception))
